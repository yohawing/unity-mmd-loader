# CI PlayMode fixture

This project intentionally contains no consumer scenes or licensed assets. The
PlayMode suite builds its fixtures from the package's redistribution-safe test
assets and runs here so CI does not depend on the ignored `unity-mmd` project.
